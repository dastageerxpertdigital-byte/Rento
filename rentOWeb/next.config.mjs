/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'i.pravatar.cc', 'images.unsplash.com'],
    },
};

export default nextConfig;
